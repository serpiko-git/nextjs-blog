module.exports = {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx}",
    "./components/**/*.{js,ts,jsx,tsx}",
    // For the best performance and to avoid false positives, 최상의 성능과 오탐 방지를 위해,
    // be as specific as possible with your content configuration. 콘첸트 구성은 가능한 한 구체적이어야 한다.
  ],
};
